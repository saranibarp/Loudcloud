<h2>Listen to our new songs!</h2>

<div class="row">
  <div class="one-half column">
    <ul class="song-list">
      <% @songs.each do |song| %>
        <%= render song %>
      <% end %>
    </ul>
  </div>
</div>